name: tithe_calculator
description: Increase-based Tithe Calculator

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^0.3.10
  flutter_slidable: ^0.5.4
  moor_flutter: ^2.1.1
  cupertino_icons: ^0.1.2
  provider: ^4.1.0
  flutter_masked_text: ^0.8.0  

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^2.4.0
  build_runner:
  flutter_launcher_icons: ^0.7.5

flutter_icons:
  image_path: "assets/copy.png"
  android: true
  ios: true

flutter:
  uses-material-design: true